#!/bin/sh
# Recommended committed hook wrapper to copy into .git/hooks
node scripts/check-secrets.js
status=$?
if [ $status -ne 0 ]; then
  echo "pre-commit: secret scan failed. Fix issues or bypass with 'git commit -n'"
  exit $status
fi
exit 0
