# Production environment example (DO NOT commit real secrets).
# Copy this to .env.production locally (or set the values in your host control panel)
# and replace the placeholders with your real production values.

# App / runtime
NODE_ENV=production
NEXTAUTH_URL=https://your-production-domain.com
NEXTAUTH_SECRET=<REPLACE_WITH_STRONG_SECRET>
NEXT_PUBLIC_BASE_URL=https://your-production-domain.com

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://<your-supabase-project>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<REPLACE_WITH_SUPABASE_ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<REPLACE_WITH_SERVICE_ROLE_KEY>  # server-only, never public

# Payments (Paystack or other)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=<REPLACE_WITH_PAYSTACK_PUBLIC_KEY>
PAYSTACK_SECRET_KEY=<REPLACE_WITH_PAYSTACK_SECRET_KEY>  # server-only

# Cloudinary (if used)
NEXT_PUBLIC_CLOUDINARY_URL=https://api.cloudinary.com/v1_1/<cloud_name>/image/upload
CLOUDINARY_API_KEY=<REPLACE_WITH_CLOUDINARY_API_KEY>
CLOUDINARY_API_SECRET=<REPLACE_WITH_CLOUDINARY_API_SECRET>

# Admin emails (comma separated) - used to mark admin accounts
ADMIN_EMAILS=info.laserstarglobal@gmail.com,amaranwiru@gmail.com

# Optional toggles (keep false in production)
DEV_ADMIN_BYPASS=false
NEXT_PUBLIC_DEV_ADMIN_BYPASS=false

# Guidance:
# - Never store real secrets in repository. Use your host's secret manager or environment settings.
# - After setting these values on the host, run your usual build/deploy pipeline.
# - For local production testing, copy this file to .env.production and populate values, then run:
#     NODE_ENV=production NEXTAUTH_URL=https://your-production-domain.com NEXTAUTH_SECRET=<secret> npm run build
#     NODE_ENV=production NEXTAUTH_URL=https://your-production-domain.com npm run start
